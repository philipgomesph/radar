generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model TabelaTest {
  id    String @id @default(uuid())
  nome  String
  idade Int

  @@map("tabela_test")
}
